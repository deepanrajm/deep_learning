# ═══════════════════════════════════════════════════════════════
# Agentic AI Workshop — Pinned Requirements
# Python: 3.10.x  (tested; also works on 3.11)
#
# conda create -n agentic_ai python=3.10 -y
# conda activate agentic_ai
# pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════════

# ── Core LangChain stack (pinned to 0.3.x — stable, well-documented) ──
langchain==0.3.25
langchain-core==0.3.60
langchain-openai==0.3.15
langchain-community==0.3.24
langchain-text-splitters==0.3.8

# ── LangGraph (multi-agent orchestration) ──────────────────────
langgraph==0.3.34
langgraph-checkpoint==2.1.2
langgraph-prebuilt==0.1.8

# ── OpenAI-compatible HTTP client ──────────────────────────────
openai==1.82.0

# ── HTTP client (for CoinGecko, Wikipedia live tools) ──────────
httpx==0.27.2

# ── Token counting ─────────────────────────────────────────────
tiktoken==0.9.0

# ── FiftyOne (Section 6 — dataset analysis agent) ──────────────
fiftyone==0.25.2
torch==2.5.1                  # required by fiftyone zoo datasets
torchvision==0.20.1           # required by fiftyone to download CIFAR-10

# ── SerpAPI — Section 2 live search tool ───────────────────────
google-search-results==2.4.2  # serpapi python client (load_tools["serpapi"])

# ── Math tool backend — required by load_tools["llm-math"] ─────
numexpr==2.10.2               # faster math expression evaluator used by llm-math

# ── Fine-tuning libraries (Section 7 — GPU optional) ───────────
transformers==4.46.3
datasets==3.1.0
peft==0.13.2
trl==0.11.4
accelerate==1.1.1

# ── Jupyter / notebook runtime ─────────────────────────────────
notebook==7.3.2
ipykernel==6.29.5
ipywidgets==8.1.5

# ── Utilities ──────────────────────────────────────────────────
python-dotenv==1.0.1
pydantic==2.10.3